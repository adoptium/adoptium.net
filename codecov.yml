# Some portions generated by Co-Pilot
coverage:
  status:
    project:
      default:
        informational: true # reports coverage but never fails the PR
    patch:
      default:
        informational: true # reports coverage but never fails the PR
