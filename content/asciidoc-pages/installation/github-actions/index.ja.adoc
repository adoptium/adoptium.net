// Some portions generated by Co-Pilot
= GitHub Actions での Eclipse Temurin の使用
:page-authors: gdams
:page-based-on: 500ef6794002c89ca2de1fe436c006e017eda37a
:toc:
:icons: font
:setup-java-version: v5
:checkout-version: v6

link:https://github.com/actions/setup-java[`actions/setup-java`] アクションは、GitHub Actions ワークフローで Eclipse Temurin をインストールするための推奨方法です。このアクションはランナー上での JDK のダウンロード、キャッシュ、設定を処理します。

== 基本的な使用方法

ワークフローで Eclipse Temurin をセットアップするには、次のステップを追加します：

[NOTE]
====
これらの例に示されているアクションのバージョンは、常に最新リリースを反映しているわけではありません。最新バージョンについては link:https://github.com/actions/setup-java/releases[`actions/setup-java` リリース]および link:https://github.com/actions/checkout/releases[`actions/checkout` リリース]を確認してください。Adoptium プロジェクトは、セキュリティと再現性を向上させるために、バージョンタグではなく完全なコミット SHA にアクションをピン留めすることを推奨しています。
====

[source, yaml, subs="attributes"]
----
steps:
  - uses: actions/checkout@{checkout-version}
  - uses: actions/setup-java@{setup-java-version}
    with:
      distribution: 'temurin'
      java-version: '{latest-lts}'
  - run: java --version
----

`distribution` は `temurin` に設定し、`java-version` でインストールするメジャーバージョンを指定します。

== 複数の Java バージョンに対するテスト

マトリックス戦略を使用して、複数の Java バージョンに対してプロジェクトをテストします：

[source, yaml, subs="attributes"]
----
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        java: [ '11', '17', '{latest-lts}' ]
    name: Java ${{ matrix.java }}
    steps:
      - uses: actions/checkout@{checkout-version}
      - uses: actions/setup-java@{setup-java-version}
        with:
          distribution: 'temurin'
          java-version: ${{ matrix.java }}
      - run: java --version
----

== 依存関係のキャッシュ

`setup-java` アクションには Maven、Gradle、sbt によって管理される依存関係のキャッシュのサポートが組み込まれています。

=== Maven 依存関係のキャッシュ

[source, yaml, subs="attributes"]
----
steps:
  - uses: actions/checkout@{checkout-version}
  - uses: actions/setup-java@{setup-java-version}
    with:
      distribution: 'temurin'
      java-version: '{latest-lts}'
      cache: 'maven'
  - run: mvn -B package
----

=== Gradle 依存関係のキャッシュ

[source, yaml, subs="attributes"]
----
steps:
  - uses: actions/checkout@{checkout-version}
  - uses: actions/setup-java@{setup-java-version}
    with:
      distribution: 'temurin'
      java-version: '{latest-lts}'
      cache: 'gradle'
  - run: ./gradlew build
----

=== sbt 依存関係のキャッシュ

[source, yaml, subs="attributes"]
----
steps:
  - uses: actions/checkout@{checkout-version}
  - uses: actions/setup-java@{setup-java-version}
    with:
      distribution: 'temurin'
      java-version: '{latest-lts}'
      cache: 'sbt'
  - run: sbt package
----

== 完全なワークフローの例

複数の Java バージョンで Eclipse Temurin を使用して Maven プロジェクトをビルドおよびテストする完全なワークフローを以下に示します：

[source, yaml, subs="attributes"]
----
name: Build and Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        java: [ '11', '17', '{latest-lts}' ]
    name: Build with Java ${{ matrix.java }}
    steps:
      - uses: actions/checkout@{checkout-version}
      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@{setup-java-version}
        with:
          distribution: 'temurin'
          java-version: ${{ matrix.java }}
          cache: 'maven'
      - name: Build with Maven
        run: mvn -B package --file pom.xml
----

== 最新バージョンの使用

`check-latest: true` を設定すると、ランナーにキャッシュされたバージョンではなく、常に最新の利用可能なバージョンをダウンロードします：

[source, yaml, subs="attributes"]
----
- uses: actions/setup-java@{setup-java-version}
  with:
    distribution: 'temurin'
    java-version: '{latest-lts}'
    check-latest: true
----

NOTE: `check-latest` を `true` に設定するとパフォーマンスへの影響があります。JDK のダウンロードはキャッシュされたバージョンの使用よりも低速です。

== 複数の JDK のインストール

1 つのジョブに複数の JDK バージョンをインストールできます。最後に設定されたバージョンが `PATH` のデフォルトになります：

[source, yaml, subs="attributes"]
----
steps:
  - uses: actions/setup-java@{setup-java-version}
    with:
      distribution: 'temurin'
      java-version: |
        11
        17
        {latest-lts}
----

他の Java バージョンには `JAVA_HOME_11_X64`、`JAVA_HOME_17_X64` などの環境変数を通じてアクセスできます。

== 参考情報

* link:https://github.com/actions/setup-java[GitHub 上の `actions/setup-java`]
* link:https://github.com/actions/setup-java/blob/main/docs/advanced-usage.md[高度な使用方法のドキュメント]
