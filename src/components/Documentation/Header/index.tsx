// Some portions generated by Co-Pilot
"use client";

import React from "react";
import DocumentationSearch from "@/components/Documentation/Search";
import DocThemeToggle from "@/components/DocThemeToggle";
import { useTranslations } from "next-intl";

const DocumentationHeader: React.FC = () => {
  const t = useTranslations("DocsPage");
  return (
    <div className="relative pt-4 md:pt-8 light:bg-white">
      <div className="border-b border-white/10 light:border-gray-200">
        <div className="mx-auto max-w-[1200px] w-full px-6 lg:px-8 py-6">
          {/* Top row: search + toggle */}
          <div className="flex items-center justify-between gap-4 mb-4">
            <DocumentationSearch compact />
            <DocThemeToggle />
          </div>

          {/* Breadcrumb */}
          <nav
            className="flex items-center text-sm text-gray-400 light:text-gray-500 mb-3"
            aria-label="Breadcrumb"
          >
            <a
              href="/"
              className="hover:text-white light:hover:text-gray-900 transition-colors"
            >
              {t("home")}
            </a>
            <svg
              className="w-4 h-4 mx-1.5 text-gray-600 light:text-gray-400"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path
                strokeLinecap="round"
                strokeLinejoin="round"
                strokeWidth={2}
                d="M9 5l7 7-7 7"
              />
            </svg>
            <span className="text-gray-200 light:text-gray-900 font-medium">
              {t("documentation")}
            </span>
          </nav>

          {/* Title + subtitle */}
          <h1 className="text-2xl md:text-3xl font-semibold text-white light:text-gray-900 tracking-tight">
            {t("title")}
          </h1>
          <p className="mt-1.5 text-sm text-gray-400 light:text-gray-500">
            {t("subtitle")}
          </p>
        </div>
      </div>
    </div>
  );
};

export default DocumentationHeader;
